# ──────────────────────────────────────────────────────────────────────────────
#  .env.example  –  Copy to .env and adjust if needed.
#  All defaults work out-of-the-box with LocalStack (no changes required).
# ──────────────────────────────────────────────────────────────────────────────

# AWS / LocalStack
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1
AWS_ENDPOINT_URL=http://localhost:4566

# Spark
SPARK_MASTER_URL=local[*]

# Pipeline paths (S3)
S3_BUCKET=pipeline-data
BRONZE_PATH=s3a://pipeline-data/bronze/sales
SILVER_PATH=s3a://pipeline-data/silver/sales
GOLD_PATH=s3a://pipeline-data/gold

# Source data glob (after generate_data.py uploads)
SOURCE_PATH=s3a://pipeline-data/bronze/sales/*/*/sales_raw.csv
